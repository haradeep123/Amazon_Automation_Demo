<?xml version="1.0" encoding="UTF-8"?>
<suite name="SanityTestSuite" parallel="false" verbose="2">
    
    <!-- Listeners for retry functionality -->
    <listeners>
        <listener class-name="com.example.utils.RetryListener"/>
    </listeners>
    
    <!-- Global parameters -->
    <parameter name="browser" value="chrome"/>
    <parameter name="timeout" value="8"/>
    
    <!-- Sanity Tests - Quick health check -->
    <test name="SanityTests">
        <groups>
            <run>
                <include name="sanity"/>
                <include name="quick"/>
            </run>
        </groups>
        
        <classes>
            <class name="com.example.tests.AmazonAutomationTest"/>
            <class name="com.example.tests.BrokenLinksTest"/>
            <class name="com.example.tests.RetryDemoTest"/>
        </classes>
    </test>
    
</suite>
